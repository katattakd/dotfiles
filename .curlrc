# Enable http compression
--compressed
--tr-encoding

# If the output dir does not exist, create it.
--create-dirs

# Fail on the first error
--fail-early

# Follow redirects
--location

# If multiple files are specified, download them all in parallel
--parallel

# Allow creating evil paths with ".." in them. Useful for developing http servers.
--path-as-is

# Default to HTTPS
--proto-default https

# Set the file timestamps to the time specified in HTTP headers
--remote-time

# Retry if a transient error (5xx status code or connection issue) occurs
--retry 3

# Use TCP fast open if possible
--tcp-fastopen

# Only allow TLS 1.2 or greater
--tlsv1.2

# Show timestamps
--trace-time

# Show protocol info
--verbose

# Print a breakdown of the response time and transfer size
--write-out "\n  size_request:  %{size_request}\n   size_upload:  %{size_upload}\n  speed_upload:  %{speed_upload}\n               ----------\n   size_header:  %{size_header}\n size_download:  %{size_download}\nspeed_download:  %{speed_download}\n\n      time_dns:  %{time_namelookup}\n  time_connect:  %{time_connect}\n time_redirect:  %{time_redirect}\n      time_tls:  %{time_appconnect}\n  time_headers:  %{time_pretransfer}\ntime_firstbyte:  %{time_starttransfer}\n               ----------\n    time_total:  %{time_total}\n"
